# ARPA — install on a new machine:
#   python -m venv .venv
#   .venv\Scripts\activate        # Windows
#   source .venv/bin/activate     # macOS/Linux
#   pip install -r requirements.txt
#   pip install -e .
#
# Copy .env.example to .env and add your Gemini API key (or use Ollama).

# Core
pydantic>=2.6
pydantic-settings>=2.2
httpx>=0.27
docker>=7.0
loguru>=0.7
pypdf>=4.0

# ML — needed for dataset loading and local verification
torch>=2.2
torchvision>=0.17
datasets>=2.18
